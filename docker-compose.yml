services:
  permanent-enrichment:
    build: .
    container_name: permanent-enrichment
    ports:
      - "8501:8501"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
