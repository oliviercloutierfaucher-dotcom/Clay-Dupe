---
phase: 13-cloud-deployment-pipeline-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified: []
autonomous: false
requirements:
  - INFRA-02

user_setup:
  - service: railway
    why: "Cloud deployment platform for the application"
    env_vars:
      - name: APP_PASSWORD
        source: "Choose a strong password for the auth gate"
      - name: APOLLO_API_KEY
        source: "Apollo.io dashboard"
      - name: FINDYMAIL_API_KEY
        source: "Findymail dashboard"
      - name: ICYPEAS_API_KEY
        source: "Icypeas dashboard"
      - name: CONTACTOUT_API_KEY
        source: "ContactOut dashboard"
      - name: DATAGMA_API_KEY
        source: "Datagma dashboard"
      - name: ANTHROPIC_API_KEY
        source: "Anthropic console"
      - name: SALESFORCE_USERNAME
        source: "Salesforce credentials"
      - name: SALESFORCE_PASSWORD
        source: "Salesforce credentials"
      - name: SALESFORCE_SECURITY_TOKEN
        source: "Salesforce credentials"
    dashboard_config:
      - task: "Create new project from GitHub repo"
        location: "Railway Dashboard -> New Project -> Deploy from GitHub Repo"
      - task: "Add persistent volume mounted at /data"
        location: "Service -> Settings -> Volumes -> Mount path: /data"
      - task: "Generate public domain"
        location: "Service -> Settings -> Networking -> Generate Domain"
      - task: "Configure health check path"
        location: "Service -> Settings -> Health Check -> Path: /_stcore/health"
      - task: "Set all environment variables from .env.example"
        location: "Service -> Variables -> Add all 13 env vars (DB_PATH=/data/clay_dupe.db)"

must_haves:
  truths:
    - "Application is accessible via Railway URL"
    - "Auth gate blocks unauthenticated access on Railway"
    - "All pages load correctly in cloud environment"
    - "Data persists across Railway redeploys (volume mount working)"
  artifacts: []
  key_links: []
---

<objective>
Deploy the application to Railway and verify it works end-to-end in the cloud using the SMOKE_TEST.md checklist.

Purpose: Complete the INFRA-02 requirement by proving the app runs in the cloud. This is the final phase of v2.0.
Output: Live Railway deployment verified with smoke test
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-cloud-deployment-pipeline-polish/13-01-SUMMARY.md
@SMOKE_TEST.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy to Railway</name>
  <files>N/A</files>
  <action>
User deploys the application to Railway. Claude has already prepared all Docker production config, CI pipeline, and E2E tests in Plan 01.

Steps for the user:
1. Push the main branch to GitHub (merge current branch if needed)
2. Create a new Railway project from the GitHub repo (Railway Dashboard -> New Project -> Deploy from GitHub Repo)
3. Configure Railway settings:
   - Add persistent volume: Service -> Settings -> Volumes -> Mount path: /data
   - Generate domain: Service -> Settings -> Networking -> Generate Domain
   - Set health check: Service -> Settings -> Health Check -> Path: /_stcore/health
   - Add all 13 environment variables from .env.example (set DB_PATH=/data/clay_dupe.db)
4. Wait for first deployment to complete (Railway auto-builds from Dockerfile)
5. Visit the generated Railway URL -- app should show the auth gate
  </action>
  <verify>Railway deployment shows "Active" status and URL is accessible</verify>
  <done>Application is deployed to Railway and accessible via URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run smoke test checklist</name>
  <files>N/A</files>
  <action>
User runs through the SMOKE_TEST.md checklist at the Railway URL to verify the deployment works end-to-end:
1. Auth gate blocks without password, grants access with correct APP_PASSWORD
2. All 5 pages load: Companies, Enrich, Emails, Analytics, Settings
3. Settings page shows API key validation status
4. Can import a CSV of companies
5. Health endpoint returns "ok": curl https://RAILWAY_URL/_stcore/health
  </action>
  <verify>All SMOKE_TEST.md checklist items pass</verify>
  <done>Smoke test passes -- app is live, authenticated, all pages load, health check returns ok</done>
</task>

</tasks>

<verification>
1. Railway deployment shows "Active" status
2. App accessible via Railway URL with auth gate
3. All SMOKE_TEST.md items checked
4. Health endpoint returns "ok"
</verification>

<success_criteria>
- Application is live on Railway and accessible via URL
- Auth gate works in cloud environment
- All pages load and core functionality works
- Data persists (volume mount confirmed)
- Full v2.0 pipeline operational in the cloud
</success_criteria>

<output>
After completion, create `.planning/phases/13-cloud-deployment-pipeline-polish/13-02-SUMMARY.md`
</output>
