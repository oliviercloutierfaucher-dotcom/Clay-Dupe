---
phase: 10-infrastructure-company-sourcing
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - ui/pages/search.py
  - ui/pages/companies.py
  - data/io.py
  - tests/test_company_sourcing.py
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-05

must_haves:
  truths:
    - "User can search Apollo for companies with ICP filters and save results to DB"
    - "User can import a CSV of companies that maps columns and saves to DB with source_type='csv_import'"
    - "User can manually add a company via form with source_type='manual'"
    - "Source type is tracked per company record (apollo_search, csv_import, manual)"
    - "Companies are auto-merged by domain on import/save"
  artifacts:
    - path: "ui/pages/search.py"
      provides: "Extended search page that saves company results to DB"
      contains: "upsert_company"
    - path: "ui/pages/companies.py"
      provides: "New company list page with CSV import and manual add"
      min_lines: 100
    - path: "data/io.py"
      provides: "Extended ColumnMapper with company-specific aliases"
      contains: "revenue"
    - path: "tests/test_company_sourcing.py"
      provides: "Tests for CSV import, manual add, source tracking"
  key_links:
    - from: "ui/pages/search.py"
      to: "data/database.py"
      via: "upsert_company() after Apollo search results selected"
      pattern: "upsert_company"
    - from: "ui/pages/companies.py"
      to: "data/io.py"
      via: "ColumnMapper for CSV import column mapping"
      pattern: "ColumnMapper"
    - from: "ui/pages/companies.py"
      to: "data/database.py"
      via: "upsert_company() for CSV import and manual add"
      pattern: "upsert_company"
---

<objective>
Build the three company sourcing channels: Apollo search (extend existing), CSV import, and manual add. Create the company list page.

Purpose: Users need to source companies from multiple channels before discovering contacts and enriching. This plan creates all three input paths and the persistent company database view. CSV import is the primary source (covers Grata/Inven/Ocean.io lists).

Output: Extended search page that saves companies to DB, new companies page with CSV import + manual add form + sortable/filterable company table.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-infrastructure-company-sourcing/10-CONTEXT.md
@.planning/phases/10-infrastructure-company-sourcing/10-RESEARCH.md
@.planning/phases/10-infrastructure-company-sourcing/10-01-SUMMARY.md

@ui/pages/search.py
@data/io.py
@data/models.py
@data/database.py
@ui/app.py
@config/settings.py

<interfaces>
<!-- From Plan 01 (must be complete before this plan runs) -->

From data/models.py (updated by Plan 01):
```python
class Company(BaseModel):
    id: str
    name: str | None = None
    domain: str | None = None
    industry: str | None = None
    employee_count: int | None = None
    country: str | None = None
    source_provider: Optional[ProviderName] = None
    source_type: Optional[str] = None       # NEW: "apollo_search", "csv_import", "manual"
    icp_score: Optional[int] = None          # NEW: 0-100
    status: str = "new"                      # NEW: "new", "contacted", "skipped"
```

From data/database.py (updated by Plan 01):
```python
class Database:
    async def upsert_company(self, company: Company) -> Company: ...
    async def search_companies(self, **filters) -> list[Company]: ...
    # search_companies now supports: status, source_type, min_icp_score filters
```

From data/io.py:
```python
class ColumnMapper:
    KNOWN_ALIASES: dict[str, list[str]]  # 70+ column aliases
    def detect_columns(self, df: pd.DataFrame) -> dict[str, str]: ...
    def apply_mapping(self, df: pd.DataFrame, mapping: dict) -> pd.DataFrame: ...

def read_input_file(path: str) -> pd.DataFrame: ...
```

From ui/app.py:
```python
def get_database() -> Database: ...
def get_settings() -> Settings: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend search page + CSV column mapper for companies</name>
  <files>ui/pages/search.py, data/io.py, tests/test_company_sourcing.py</files>
  <behavior>
    - Test: ColumnMapper detects company-specific columns (revenue, ebitda, founded_year, employee_count, website)
    - Test: CSV with company data maps to Company model fields correctly
    - Test: Imported companies have source_type="csv_import"
    - Test: Apollo-sourced companies have source_type="apollo_search"
    - Test: Manual companies have source_type="manual"
    - Test: Domain normalization deduplicates on import (www.acme.com and acme.com merge)
  </behavior>
  <action>
1. Create `tests/test_company_sourcing.py`:
   - Test ColumnMapper with company-specific aliases (revenue_usd, annual_revenue, etc.)
   - Test CSV -> Company model mapping with various column name formats
   - Test source_type assignment for each source channel
   - Test domain normalization: "www.acme.com", "https://acme.com/", "acme.com" all normalize to "acme.com"
   - Test duplicate handling: two CSV rows with same domain result in one company (COALESCE merge)

2. Update `data/io.py` ColumnMapper.KNOWN_ALIASES:
   - Add company-specific mappings:
     - "revenue_usd": ["revenue", "annual_revenue", "annual_revenue_usd", "yearly_revenue", "rev"]
     - "ebitda_usd": ["ebitda", "annual_ebitda", "ebitda_usd"]
     - "founded_year": ["founded", "year_founded", "founding_year", "established"]
     - "employee_count": add aliases ["headcount", "num_employees", "total_employees", "employees", "size"]
     - "website_url": ["website", "url", "company_url", "web", "homepage"]
     - "linkedin_url": add aliases ["linkedin", "linkedin_profile", "company_linkedin"]
     - "description": ["about", "company_description", "overview", "summary"]
   - Add `map_to_company(df: pd.DataFrame, mapping: dict) -> list[Company]`:
     - Takes mapped DataFrame, creates Company objects with source_type set
     - Runs through Company model constructor for domain normalization

3. Update `ui/pages/search.py`:
   - After Apollo company search results are displayed, add "Save Selected to Database" button
   - On click: iterate selected companies, set source_type="apollo_search", call upsert_company() for each
   - Show success message with count of saved/merged companies
   - Preserve existing search functionality unchanged
  </action>
  <verify>
    <automated>pytest tests/test_company_sourcing.py tests/test_io.py -x --timeout=30</automated>
  </verify>
  <done>ColumnMapper handles company-specific CSV columns, search page saves Apollo results to DB with source_type tracking, CSV data maps correctly to Company models</done>
</task>

<task type="auto">
  <name>Task 2: Company list page with CSV import and manual add</name>
  <files>ui/pages/companies.py, ui/app.py</files>
  <action>
1. Create `ui/pages/companies.py` following existing Streamlit page patterns (search.py, results.py):
   - Page header: "Companies"

   **Company Table (main section):**
   - Fetch companies from DB using `run_sync(db.search_companies(**filters))`
   - Display as st.dataframe with sortable columns: Name, Domain, Industry, Employees, Country, Source, ICP Score, Status
   - Filter row at top with 4 columns:
     - Status filter: selectbox ["All", "new", "contacted", "skipped"]
     - Source filter: selectbox ["All", "apollo_search", "csv_import", "manual"]
     - Min ICP Score: slider 0-100
     - Search: text_input for name/domain search
   - Show total count of matching companies

   **CSV Import (sidebar or expander section):**
   - st.file_uploader accepting .csv, .xlsx, .xls
   - On upload: read with `read_input_file()`, detect columns with ColumnMapper
   - Show column mapping preview (auto-detected + manual override dropdowns)
   - "Import" button: map columns, create Company objects with source_type="csv_import", upsert each
   - Show progress bar during import
   - Show results: N imported, M merged (existing domain), K errors

   **Manual Add (sidebar or expander section):**
   - Form with fields: Company Name (required), Domain, Industry, Employee Count, Country, Description
   - Country dropdown with common values: US, CA, UK, IE (per user decision), plus free text
   - On submit: create Company with source_type="manual", upsert to DB
   - Show success/error message

   **Status Management:**
   - Allow changing status of selected companies (new -> contacted, new -> skipped, etc.)
   - Bulk status update via multiselect + action button

2. Register page in `ui/app.py`:
   - Add companies page to st.navigation: `st.Page("pages/companies.py", title="Companies", icon=":material/business:")`
   - Place after Search in navigation order
  </action>
  <verify>
    <automated>python -c "import ui.pages.companies; print('companies page imports OK')" && pytest tests/test_company_sourcing.py -x --timeout=30</automated>
  </verify>
  <done>Company list page shows sortable/filterable table, CSV import works with column auto-detection, manual add form creates companies, all sources tracked correctly, page registered in app navigation</done>
</task>

</tasks>

<verification>
- `pytest tests/test_company_sourcing.py tests/test_io.py -x --timeout=30` all pass
- Companies page imports without error
- CSV import maps columns and creates companies with source_type="csv_import"
- Apollo search results save with source_type="apollo_search"
- Manual add creates companies with source_type="manual"
- Company table shows all sourced companies with filters
</verification>

<success_criteria>
- Three sourcing channels functional: Apollo search save, CSV import, manual add
- Source type tracked per company record
- Auto-merge by domain when same company appears from multiple sources
- Company list page displays all companies with sortable columns and filters
- CSV import handles various column name formats via ColumnMapper
</success_criteria>

<output>
After completion, create `.planning/phases/10-infrastructure-company-sourcing/10-03-SUMMARY.md`
</output>
