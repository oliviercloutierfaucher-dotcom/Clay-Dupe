---
phase: 12.1-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/salesforce.py
  - cli/main.py
  - providers/icypeas.py
  - providers/contactout.py
  - Dockerfile
  - docker-compose.yml
  - .env.example
  - tests/test_security_bugfixes.py
autonomous: true
requirements: [SEC-02, SEC-03, BUG-01, BUG-02, BUG-03, OPS-02]

must_haves:
  truths:
    - "SOQL queries escape user-supplied domain values before embedding in LIKE clauses"
    - "CLI enrich and resume commands create circuit breakers and rate limiters without crashing"
    - "Icypeas bulk polling exits after 300 seconds maximum"
    - "ContactOut batch polling exits after 60 iterations maximum"
    - "Dockerfile does not disable CORS or XSRF protection"
    - "Docker container runs as non-root user"
    - ".env.example lists all required environment variables including DATAGMA_API_KEY, ANTHROPIC_API_KEY, SALESFORCE_*, APP_PASSWORD"
  artifacts:
    - path: "providers/salesforce.py"
      provides: "SOQL escaping via _escape_soql() helper"
      contains: "_escape_soql"
    - path: "cli/main.py"
      provides: "Fixed factory function calls with zero arguments"
      contains: "create_circuit_breakers()"
    - path: "providers/icypeas.py"
      provides: "Timeout guard on bulk polling loop"
      contains: "monotonic"
    - path: "providers/contactout.py"
      provides: "Max-iteration guard on batch polling"
      contains: "MAX_POLL_ITERATIONS"
    - path: "Dockerfile"
      provides: "Non-root user directive"
      contains: "USER"
    - path: ".env.example"
      provides: "Complete variable listing"
      contains: "ANTHROPIC_API_KEY"
    - path: "tests/test_security_bugfixes.py"
      provides: "Unit tests for SOQL escaping, CLI args, polling guards"
  key_links:
    - from: "providers/salesforce.py"
      to: "_escape_soql"
      via: "LIKE clause construction"
      pattern: "_escape_soql\\(d\\)"
    - from: "cli/main.py"
      to: "quality/circuit_breaker.py"
      via: "zero-argument factory calls"
      pattern: "create_circuit_breakers\\(\\)"
---

<objective>
Fix security vulnerabilities, functional bugs, and Docker hardening issues identified by the production readiness audit.

Purpose: Eliminate 6 of 8 critical/high blockers before cloud deployment -- SOQL injection, CLI crash, infinite polling loops, CORS/XSRF, non-root Docker, and incomplete .env.example.
Output: Patched provider files, fixed CLI, hardened Dockerfile, complete .env.example, and unit tests proving each fix.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-production-hardening/12.1-CONTEXT.md
@.planning/phases/12.1-production-hardening/12.1-RESEARCH.md

<interfaces>
<!-- Key functions and patterns the executor needs -->

From quality/circuit_breaker.py:
```python
def create_circuit_breakers() -> dict[str, CircuitBreaker]:
    """Factory -- takes ZERO arguments. Returns dict keyed by provider name."""

def create_rate_limiters() -> dict[str, RateLimiter]:
    """Factory -- takes ZERO arguments. Returns dict keyed by provider name."""
```

From providers/salesforce.py (line 148, current vulnerable code):
```python
like_clauses = " OR ".join(
    f"Website LIKE '%{d}%'" for d in chunk
)
```

From providers/icypeas.py (line 207, no timeout):
```python
while len(collected_items) < total_expected:
    await asyncio.sleep(poll_interval)
    poll_interval = min(poll_interval * 1.5, max_interval)
```

From providers/contactout.py (line 355, no max iterations):
```python
while True:
    await asyncio.sleep(poll_interval)
    poll_interval = min(poll_interval * 1.5, max_interval)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix SOQL injection, CLI crash, and polling guards with tests</name>
  <files>providers/salesforce.py, cli/main.py, providers/icypeas.py, providers/contactout.py, tests/test_security_bugfixes.py</files>
  <behavior>
    - test_escape_soql_single_quotes: _escape_soql("O'Brien") returns "O\\'Brien"
    - test_escape_soql_backslashes: _escape_soql("a\\b") escapes backslash first
    - test_escape_soql_like_wildcards: _escape_soql("50%_off") escapes % and _
    - test_escape_soql_control_chars: _escape_soql("ab\x00cd") strips null bytes
    - test_escape_soql_clean_domain: _escape_soql("example.com") returns "example.com" unchanged
    - test_cli_circuit_breakers_no_args: create_circuit_breakers() callable with zero args (already true, just verify)
    - test_cli_rate_limiters_no_args: create_rate_limiters() callable with zero args (already true, just verify)
    - test_icypeas_polling_timeout: mock polling loop, verify it exits after MAX_POLL_TIMEOUT (use small value for test)
    - test_contactout_polling_max_iterations: mock polling loop, verify it exits after MAX_POLL_ITERATIONS
  </behavior>
  <action>
1. Create `tests/test_security_bugfixes.py` with all tests listed in behavior (RED phase -- tests fail).

2. Fix `providers/salesforce.py`:
   - Add `_escape_soql(value: str) -> str` function near top of file (after imports).
   - Implementation: strip control chars via `re.sub(r'[\x00-\x1f]', '', value)`, escape backslashes first (`\\` -> `\\\\`), then single quotes (`'` -> `\\'`), then LIKE wildcards (`%` -> `\\%`, `_` -> `\\_`).
   - Add `import re` at top.
   - Update line 148 to use `_escape_soql(d)` in the LIKE clause: `f"Website LIKE '%{_escape_soql(d)}%'"`.

3. Fix `cli/main.py`:
   - Line 348: Change `create_circuit_breakers(list(providers.keys()))` to `create_circuit_breakers()`.
   - Line 349: Change `create_rate_limiters(list(providers.keys()))` to `create_rate_limiters()`.
   - Line 877: Same fix for `create_circuit_breakers()`.
   - Line 878: Same fix for `create_rate_limiters()`.

4. Fix `providers/icypeas.py`:
   - Add `import time` at top if not present.
   - Add constant `MAX_POLL_TIMEOUT = 300` near top of class or module.
   - Before the `while` loop at line 207, add `deadline = time.monotonic() + MAX_POLL_TIMEOUT`.
   - Inside the while loop, add as first check: `if time.monotonic() > deadline: logger.warning("Icypeas bulk polling timed out after %ds", MAX_POLL_TIMEOUT); break`.

5. Fix `providers/contactout.py`:
   - Add constant `MAX_POLL_ITERATIONS = 60` near top of class or module.
   - Before the `while True:` at line 355, add `iterations = 0`.
   - Inside the while loop, add: `iterations += 1` then `if iterations > MAX_POLL_ITERATIONS: logger.warning("ContactOut batch polling exceeded %d iterations", MAX_POLL_ITERATIONS); return []`.

6. Run tests (GREEN phase).
  </action>
  <verify>
    <automated>cd "C:\Users\OlivierFaucher\OneDrive - Valsoft Corporation\Claude Code Project\Clay-Dupe-repo" && python -m pytest tests/test_security_bugfixes.py -x -v</automated>
  </verify>
  <done>All 9 tests pass. SOQL escaping handles quotes/backslashes/wildcards/control chars. CLI factory calls use zero arguments. Icypeas has 300s timeout. ContactOut has 60-iteration max.</done>
</task>

<task type="auto">
  <name>Task 2: Docker hardening and .env.example completion</name>
  <files>Dockerfile, docker-compose.yml, .env.example</files>
  <action>
1. Update `Dockerfile`:
   - The current CMD does NOT have `--server.enableCORS=false` or `--server.enableXsrfProtection=false` flags (already clean per research Pitfall 5). Verify and leave CMD unchanged for SEC-03.
   - Before the `EXPOSE 8501` line, add non-root user setup:
     ```
     RUN groupadd -r appuser && useradd -r -g appuser -d /app appuser
     RUN chown -R appuser:appuser /app /data
     USER appuser
     ```
   - This satisfies OPS-02 (non-root container).

2. Verify `docker-compose.yml`:
   - Confirm no `command:` override with CORS/XSRF disable flags. Current file has no command override (already clean for SEC-03).
   - Add `.env` volume mount for settings persistence: add `- ./.env:/app/.env` under volumes so set_key() changes persist on host. (Needed for Plan 02's OPS-01 to work in Docker.)

3. Update `.env.example`:
   - Add missing variables to existing file. Final file should contain ALL of:
     ```
     APOLLO_API_KEY=
     FINDYMAIL_API_KEY=
     ICYPEAS_API_KEY=
     CONTACTOUT_API_KEY=
     DATAGMA_API_KEY=
     ANTHROPIC_API_KEY=
     WATERFALL_ORDER=apollo,icypeas,findymail,contactout
     CACHE_TTL_DAYS=30
     DB_PATH=clay_dupe.db
     SALESFORCE_USERNAME=
     SALESFORCE_PASSWORD=
     SALESFORCE_SECURITY_TOKEN=
     APP_PASSWORD=
     ```
  </action>
  <verify>
    <automated>cd "C:\Users\OlivierFaucher\OneDrive - Valsoft Corporation\Claude Code Project\Clay-Dupe-repo" && grep -c "USER appuser" Dockerfile && grep -c "DATAGMA_API_KEY" .env.example && grep -c "ANTHROPIC_API_KEY" .env.example && grep -c "APP_PASSWORD" .env.example && grep -c "SALESFORCE_USERNAME" .env.example && echo "--- Checking no CORS disable flags ---" && (grep -c "enableCORS=false" Dockerfile docker-compose.yml 2>/dev/null && echo "FAIL: CORS disabled" || echo "PASS: No CORS disable flags")</automated>
  </verify>
  <done>Dockerfile runs as non-root appuser. No CORS/XSRF disable flags in Dockerfile or docker-compose.yml. .env.example lists all 13 variables including DATAGMA_API_KEY, ANTHROPIC_API_KEY, SALESFORCE_*, APP_PASSWORD.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_security_bugfixes.py -x -v` -- all security/bug fix tests pass
- `grep -c "_escape_soql" providers/salesforce.py` returns >= 2 (definition + usage)
- `grep "create_circuit_breakers()" cli/main.py` shows zero-argument calls
- `grep "monotonic" providers/icypeas.py` confirms timeout guard
- `grep "MAX_POLL_ITERATIONS" providers/contactout.py` confirms iteration guard
- `grep "USER appuser" Dockerfile` confirms non-root
- `grep "APP_PASSWORD" .env.example` confirms complete variables
- `python -m pytest tests/ -x --timeout=60` -- full test suite still passes
</verification>

<success_criteria>
1. SOQL queries use _escape_soql() on all user-supplied domain values
2. CLI enrich and resume commands call create_circuit_breakers() and create_rate_limiters() with zero arguments
3. Icypeas bulk polling exits after MAX_POLL_TIMEOUT (300s)
4. ContactOut batch polling exits after MAX_POLL_ITERATIONS (60)
5. Dockerfile has USER appuser directive
6. No CORS/XSRF disable flags in Dockerfile or docker-compose.yml
7. .env.example lists all 13 required variables
8. All new tests pass, full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-production-hardening/12.1-01-SUMMARY.md`
</output>
